{% extends 'base.html.twig' %}

{% block title %}Nouveau Club{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Créer un nouveau club</h1>

        {{ include('club/_form.html.twig') }}

        <a href="{{ path('app_club_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
