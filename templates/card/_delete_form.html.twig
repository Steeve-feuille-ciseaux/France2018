<form method="post" action="{{ path('app_card_delete', {'id': card.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette carte ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ card.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
